server {
    # Slušaj na portu 8080 umjesto 80
    listen 8010;

<<<<<<< HEAD
# Root direktorij za statičke datoteke
=======
>>>>>>> d63e94262028d2d1af20c75a357e9889c44ced49
    root /usr/share/nginx/html;
        index index.html;

        # Za API zahtjeve - jednostavno proxy
        location /api/ {
            proxy_pass http://backend:3500/api/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_buffer_size 128k;
            proxy_buffers 4 256k;
            proxy_busy_buffers_size 256k;
        }

<<<<<<< HEAD
    # Preusmjeravanje API zahtjeva na backend kontejner
    location /api/ {
        proxy_pass http://backend:3500/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Proxy za MinIO presigned URL-ove
    location ~ ^/minio/(.*)$ {
        proxy_pass http://minio:9000/$1;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Za ostale statičke datoteke
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000";
    }

    # Sprječavanje cacheiranja za index.html
    location = /index.html {
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}
=======
        # Za MinIO proxy
        location /minio/ {
            proxy_pass http://minio:9000/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }

        # Za React Router
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
>>>>>>> d63e94262028d2d1af20c75a357e9889c44ced49
